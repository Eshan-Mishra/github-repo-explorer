# Port the server listens on (optional, defaults to 4000)
PORT=4000

# Optional GitHub personal access token.
# Without it the app uses GitHub's unauthenticated limit (60 requests/hour).
# With it, the limit rises to 5,000 requests/hour.
GITHUB_TOKEN=
